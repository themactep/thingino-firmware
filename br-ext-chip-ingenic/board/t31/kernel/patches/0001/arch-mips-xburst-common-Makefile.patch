
--- /dev/null
+++ b/arch/mips/xburst/common/Makefile
@@ -0,0 +1,12 @@
+
+# add object if need
+
+obj-y += pmon.o
+obj-y += proc-pmon.o
+obj-${CONFIG_JZ_IMEM} += proc-imem.o
+obj-y += get-cpu-features.o
+#obj-y += trace-exit.o
+obj-y += proc-exec.o
+obj-y += mxu.o
+obj-y += proc.o
+obj-y += jz_notifier.o

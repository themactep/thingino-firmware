
--- /dev/null
+++ b/drivers/video/soc_vpu/Makefile
@@ -0,0 +1,6 @@
+obj-$(CONFIG_SOC_VPU) += soc_vpu.o
+ifeq ($(CONFIG_SOC_VPU), y)
+obj-$(CONFIG_JZ_NVPU) += jz_nvpu/
+obj-$(CONFIG_VPU_HELIX) += helix/
+obj-$(CONFIG_VPU_RADIX) += radix/
+endif

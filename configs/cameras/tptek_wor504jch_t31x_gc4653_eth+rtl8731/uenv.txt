# U-Boot environment overrides for this camera (imported at image build).
# GPIO configuration does NOT belong here - it lives in thingino.json
# (and the BR2_THINGINO_LED_*/AUDIO_GPIO kopt vars for kernel LEDs/audio).

--- a/cJSON.c
+++ b/cJSON.c
@@ -1806,9 +1806,11 @@ static cJSON_bool print_object(const cJSON * const item, printbuffer * const ou
             }
             for (i = 0; i < output_buffer->depth; i++)
             {
-                *output_pointer++ = '\t';
+                *output_pointer++ = ' ';
+                *output_pointer++ = ' ';
             }
-            output_buffer->offset += output_buffer->depth;
+            /* 2 spaces per depth level instead of 1 tab */
+            output_buffer->offset += output_buffer->depth * 2;
         }

         /* print key */
@@ -1827,7 +1829,7 @@ static cJSON_bool print_object(const cJSON * const item, printbuffer * const ou
         *output_pointer++ = ':';
         if (output_buffer->format)
         {
-            *output_pointer++ = '\t';
+            *output_pointer++ = ' ';
         }
         output_buffer->offset += length;

@@ -1870,7 +1872,8 @@ static cJSON_bool print_object(const cJSON * const item, printbuffer * const ou
         size_t i;
         for (i = 0; i < (output_buffer->depth - 1); i++)
         {
-            *output_pointer++ = '\t';
+            *output_pointer++ = ' ';
+            *output_pointer++ = ' ';
         }
     }
     *output_pointer++ = '}';

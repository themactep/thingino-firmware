diff --git a/arch/Config.in.mips b/arch/Config.in.mips
index 29b4180d21..b91871916d 100644
--- a/arch/Config.in.mips
+++ b/arch/Config.in.mips
@@ -97,6 +97,16 @@ config BR2_mips_xburst
 	  to be used in order to prevent emitting these instructions.
 
 	  See http://www.ingenic.com/en/?xburst.html
+config BR2_mips_xburst2
+	bool "XBurst2"
+	depends on !BR2_ARCH_IS_64
+	select BR2_MIPS_CPU_MIPS32R2
+	select BR2_MIPS_NAN_2008
+	help
+	  The Ingenic XBurst2 core (T40, T41, A1) is a MIPS32R2
+	  microprocessor. The vendor SDK uses IEEE 754-2008 NaN
+	  encoding, so the toolchain must match with -mnan=2008.
+
 config BR2_mips4
 	bool "MIPS IV"
 config BR2_mips_64
@@ -228,8 +238,8 @@ config BR2_MIPS_ENABLE_NAN_2008
 endchoice
 
 config BR2_GCC_TARGET_NAN
-	default "legacy"	if BR2_MIPS_NAN_LEGACY
 	default "2008"		if BR2_MIPS_NAN_2008
+	default "legacy"	if BR2_MIPS_NAN_LEGACY
 
 config BR2_ARCH
 	default "mips"		if BR2_mips
@@ -255,6 +265,7 @@ config BR2_GCC_TARGET_ARCH
 	default "m6201"		if BR2_mips_m6250
 	default "p5600"		if BR2_mips_p5600
 	default "mips32r2"	if BR2_mips_xburst
+	default "mips32r2"	if BR2_mips_xburst2
 	default "mips64"	if BR2_mips_64
 	default "mips64r2"	if BR2_mips_64r2
 	default "mips64r3"	if BR2_mips_64r3

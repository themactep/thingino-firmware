#!/bin/sh

# Variables sourced via /etc/thingino.json:
# swap_enabled (true/false to determine if swap should be configured)
# swap_type (should be 'file' or 'partition')
# swap_file_path (if swap_type is 'file')
# swap_file_size (if swap_type is 'file', in M, ex. 256M)
# swap_partition_dev (if swap_type is 'partition')

CONFIG_JSON="${CONFIG_JSON:-/etc/thingino.json}"

setup_swap_partition() {
	local partition="$1"

	if [ -z "$partition" ] || [ ! -b "$partition" ]; then
		echo "Invalid swap partition: $partition" >&2
		return 1
	fi

	# Check if partition is already in fstab
	if ! grep -q "^$partition[[:space:]]\+none[[:space:]]\+swap" /etc/fstab; then
		echo "$partition none swap sw 0 0" >>/etc/fstab
		echo "Added swap entry for $partition to /etc/fstab"
	else
		echo "Swap entry for $partition already exists in /etc/fstab"
	fi

	# Check if swap is already active
	if grep -q "$partition" /proc/swaps; then
		echo "Swap partition $partition is already active"
		return 0
	fi

	# Try to enable swap directly - if it works, it's already formatted
	if swapon "$partition" 2>/dev/null; then
		echo "Enabled swap on $partition (already formatted)"
	else
		# Failed to swapon, format it and then enable
		echo "Formatting $partition as swap"
		mkswap "$partition"
		swapon "$partition"
		echo "Enabled swap on $partition (newly formatted)"
	fi

	return 0
}

setup_swap_file() {
	local swap_file="$1"
	local swap_dir=$(dirname "$swap_file")
	local size="${swap_file_size:-128M}" # Default to 128M if not specified

	if [ -z "$swap_file" ]; then
		echo "Invalid swap file path: $swap_file" >&2
		return 1
	fi

	# Check if directory exists
	if [ ! -d "$swap_dir" ]; then
		echo "Swap file directory does not exist: $swap_dir" >&2
		return 1
	fi

	# Create swap file if it doesn't exist
	if [ ! -f "$swap_file" ]; then
		echo "Creating swap file: $swap_file (size: $size)"
		fallocate -l "$size" "$swap_file"
		chmod 600 "$swap_file"
		mkswap "$swap_file"

		# Add to fstab if not already there
		if ! grep -q "^$swap_file[[:space:]]\+none[[:space:]]\+swap" /etc/fstab; then
			echo "$swap_file none swap sw 0 0" >>/etc/fstab
			echo "Added swap entry for $swap_file to /etc/fstab"
		fi

		# Enable the newly created swap file
		swapon "$swap_file"
		echo "Enabled swap on $swap_file (newly created)"
	else
		# Check if already in fstab
		if ! grep -q "^$swap_file[[:space:]]\+none[[:space:]]\+swap" /etc/fstab; then
			echo "$swap_file none swap sw 0 0" >>/etc/fstab
			echo "Added swap entry for $swap_file to /etc/fstab"
		else
			echo "Swap entry for $swap_file already exists in /etc/fstab"
		fi

		# Check if swap is already active
		if grep -q "$swap_file" /proc/swaps; then
			echo "Swap file $swap_file is already active"
			return 0
		fi

		# Try to enable swap directly - if it works, it's already formatted
		if swapon "$swap_file" 2>/dev/null; then
			echo "Enabled swap on $swap_file (already formatted)"
		else
			# Failed to swapon, format it and then enable
			echo "Formatting $swap_file as swap"
			mkswap "$swap_file"
			swapon "$swap_file"
			echo "Enabled swap on $swap_file (newly formatted)"
		fi
	fi

	return 0
}

start() {
	if [ "$swap_enabled" != "true" ]; then
		echo "Swap is not enabled."
		exit 0
	fi

	if [ -z "$swap_type" ]; then
		echo "swap_type not defined in $CONFIG_JSON." >&2
		exit 1
	fi

	if [ ! -z "$swap_file_size" ]; then
		swap_file_size=$(echo "$swap_file_size" | tr '[:lower:]' '[:upper:]')
	fi

	echo -c 15 "Configuring swap"
	case "$swap_type" in
		"partition")
			if [ -z "$swap_partition_dev" ]; then
				echo "swap_partition_dev not defined in $CONFIG_JSON. Exiting." >&2
				exit 1
			fi
			setup_swap_partition "$swap_partition_dev"
			;;
		"file")
			if [ -z "$swap_file_path" ]; then
				echo "swap_file_path not defined in $CONFIG_JSON. Exiting." >&2
				exit 1
			fi
			setup_swap_file "$swap_file_path"
			;;
		*)
			echo "Invalid swap_type: $swap_type (should be 'file' or 'partition')" >&2
			exit 1
			;;
	esac
}

case "$1" in
	start)
		start
		;;
	stop)
		true
		;;
	restart)
		start
		;;
	*)
		echo "Usage: $0 {start|stop|restart}"
		exit 1
		;;
esac

exit $?

menuconfig BR2_PACKAGE_USBNET
	bool "USB Networking Options"
	help
	  Networking Device Options

config BR2_PACKAGE_USBNET_ASIX
	bool "ASIX AX8817X USB"
	depends on BR2_PACKAGE_USBNET
	help
	  Enables ASIX AX88xxx USB 2.0 core driver

config BR2_PACKAGE_USBNET_ASIX_179_178A
	bool "ASIX AX88179_178A USB"
	depends on BR2_PACKAGE_USBNET
	help
	  Enables ASIX AX88179_178A USB 3.0 driver

config BR2_PACKAGE_USBNET_CDC_ETHER
	bool "CDC_ETHER"
	depends on BR2_PACKAGE_USBNET
	help
	  Enables CDC Ethernet over USB

config BR2_PACKAGE_USBNET_USB_DIRECT_NCM
	bool "CDC-NCM"
	depends on BR2_PACKAGE_USBNET
	help
	  Enable CDC_NCM Support over USB, provides a network ip via DHCPD to the connected device.

config BR2_PACKAGE_USBNET_USB_DIRECT_NCM_CLIENT
	bool "CDC-NCM-CLIENT"
	depends on BR2_PACKAGE_USBNET
	help
	  Enable CDC_NCM Support over USB, working as a client device.

#config BR2_PACKAGE_USBNET_USB_DIRECT_ECM
#	bool "CDC-ECM"
#	depends on BR2_PACKAGE_USBNET
#	help
#	  Enables CDC_ECM

config BR2_PACKAGE_USBNET_R8152
	bool "Realtek 8152"
	depends on BR2_PACKAGE_USBNET
	help
	  Enables support for Realtek RTL8152 based USB 2.0 network adapters

config BR2_PACKAGE_USBNET_RNDIS
	bool "RNDIS"
	depends on BR2_PACKAGE_USBNET
	help
	  Enables RNDIS over USB

config BR2_PACKAGE_USBNET_WWAN_SERIAL
	bool "WWAN via USB SERIAL"
	depends on BR2_PACKAGE_USBNET
	help
	  Enables WWAN via USB SERIAL support

config BR2_PACKAGE_LOGCAT
	bool "logcat"
	default n
	depends on BR2_THINGINO_DEV_PACKAGES
	help
	  Userspace programs for reading the Android logging
	  facility, which is used by Ingenic IMP libraries.

config BR2_PACKAGE_THINGINO_FFMPEG
	bool "Thingino FFmpeg"
	help
	  Optimized FFmpeg build to record on thingino devices
	  FFmpeg is a complete, cross-platform solution to record,
	  convert and stream audio and video.
	  http://www.ffmpeg.org

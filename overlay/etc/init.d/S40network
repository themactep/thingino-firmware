#!/bin/sh

case "$1" in
	start)
		echo "Starting network..."
		ifup -a
		;;

	stop)
		echo "Stopping network..."
		ifdown -a
		;;

	restart | reload)
		"$0" stop
		"$0" start
		;;

	*)
		echo "Usage: $0 {start|stop|restart}"
		exit 1
		;;
esac

exit 0

#!/bin/sh

. /etc/init.d/rc.common

case "$1" in
	start)
		starting
		{ ircut off > /dev/null; sleep 0.5; ircut on > /dev/null; } && ok || fail
		;;
	stop)
		true
		;;
	*)
		die 1 "Usage: $0 {start}"
		;;
esac

exit 0
